<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Books</title>
  <!-- Link to Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/books">Book Portal</a>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
             
            </li>
            <li class="nav-item">
             
                    <button type="submit" class="btn btn-danger"><a href="/auth/user/logout" class="btn btn-danger">Logout</a>
                    </button>
                
            </li>
        </ul>
    </div>
</nav>

<div class="container mt-4">
  
  <h2>Welcome to the Book Portal, <%= user.username %>!</h2>
<p>Explore our collection of books and find your next favorite read.</p>


  <% books.forEach((book) => { %>
    <div class="book">
      <h3><%= book.title %></h3>
      <img src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-M.jpg" alt="<%= book.title %>" >
      <p><%= book.author %></p>
      <p><%= book.genre %></p>
      <!-- Rest of the book details -->
  
      <div class="mt-4">
        <a href="/user/bookById/<%= book._id %>"   class="btn btn-primary">View Details</a>
      </div>
    
    </div>
  <% }) %>
</div>


</body>
</html>