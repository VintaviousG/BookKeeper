<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Book</title>
    <!-- Link to Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container mt-4">
    <h2>View Book</h2>

    <div class="card">
        <!-- Placeholder book image, replace with actual image source -->
        <img src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-M.jpg" class="card-img-top " alt="Book Cover">
        <div class="card-body">
            <h5 class="card-title"><%= book.title %></h5>
            <p class="card-text">Author: <%= book.author %></p>
            <p class="card-text">Genre: <%= book.genre %></p>
            <p class="card-text">Description: <%= book.description %></p>
            <p class="card-text">Price: $<%= book.price.toFixed(2) %></p>
            <p class="card-text">Stock: <%= book.stock %></p>
            <p class="card-text">ISBN: <%= book.isbn %></p>
        </div>
    </div>

    <a href="/books/index" class="btn btn-secondary mt-3">Back to Books</a>
</div>

<!-- Link to Bootstrap JS and Popper.js -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
